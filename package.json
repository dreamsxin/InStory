{
  "name": "instory",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:server": "npm run dev -w apps/server",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present && npm run typecheck:e2e",
    "typecheck:e2e": "tsc -p tsconfig.e2e.json",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "tsx e2e/reset-database.ts && playwright test",
    "verify:llm": "tsx scripts/verify-llm-provider.ts"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.13"

  },
  "//overrides": "next pins postcss at 8.4.31, below the fix for GHSA-6g55-p6wh-862q (High: arbitrary file read via sourceMappingURL, patched in 8.5.12). 8.5.x is semver-compatible with that pin, so this dedupes the tree onto one current copy. Remove once next ships a patched pin.",
  "overrides": {
    "postcss": "^8.5.28"
  }
}



